import {ViewStyle} from 'react-native';

export type GetStartedStylesType = {
  container: ViewStyle;
  topBlackLine: ViewStyle;
  canvasContainer: ViewStyle;
  bottomContainer: ViewStyle;
  firstTextContainer: ViewStyle;
  secondTextContainer: ViewStyle;
  buttonContainer: ViewStyle;
};
